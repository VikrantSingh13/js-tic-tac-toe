//generate a randomly sized population
//of neural nets
//and then score them
//best one get to repoduce

var ttt = () => {
  
  var X;
  var O;
  var TIE;
  
  //helper classes
  var newBoard = () => { 0 };
  
  var isEmpty = (board) => { board === 0 };
  
  var 
  
}